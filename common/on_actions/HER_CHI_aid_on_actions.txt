on_actions = {
	on_startup = {
		effect = {
			CHI = { HER_CHI_aid_initialize = yes }
			BRA = { set_variable = { BRA_CHI_pending_infantry_equipment = 0 } }
		}
	}

	on_daily_CHI = {
		effect = {
			if = {
				limit = {
					has_country_flag = CHI_usa_tungsten_credit_active
				}
				HER_CHI_foreign_aid_daily = yes
			}
		}
	}
}
